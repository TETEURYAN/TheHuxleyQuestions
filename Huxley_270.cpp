#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
//270 - EXTREMAMENTE BÁSICO.
int main ()
{
	int A,B,X;
	scanf("%i", &A);
	scanf("%i", &B);
	X = A + B;
	printf("X = %i", X);
	return 0;
}